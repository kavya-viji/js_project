var x=prompt("Enter the boat speed");
x=parseInt(x);
var y=prompt("Enter the stream speed");
y=parseInt(y);
var downstreamSpeed;
downstreamSpeed=x+y;
var z=prompt("Enter the Distance")
z=parseInt(z);
var time;
time=z/downstreamSpeed;
alert (time);